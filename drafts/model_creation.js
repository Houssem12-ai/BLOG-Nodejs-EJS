/* const userSchema = new mongoose.schema({
    field:{
        type, unique, lowercase, required 
    }
})

const tankos = new Tank({
    name: ,
    akflkfda
})

tankos.save((err)=>{
    handleerror(err)
})
////////////////////////

model.create({"fields or object to put here "},(err)=>{
    if(err){
        handleerror(err)
    }
});

///////=> and here to insert a batch of documents
model.insertMany([{},{},{}],function(err) => {
    if(err){
        handleerror(errr)
    }
})

const User = mongoose.model('user',userSchema) */

/// fazet el custom connection
// const some = connection.mode instead of mongoose.mode

/* some.find({size:"small"}).where("createAtData").gt(192).lte(;,;FileSystemDirectoryEntry,).exec((callback)=>{
    if(callback){
        console.logadkfpdok
    }
}) */

// instead of

//some.find({size:{let:38u342,eql:384u9134}})